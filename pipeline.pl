#!/usr/bin/perl
use strict;
use warnings;
use File::Slurp;
use File::Temp;
use Getopt::Long;

GetOptions ("a|alignment=s" => \$alignment, "db|db_dir=s" => \$db_dir );

#run splitgene

#run blast

#run splitgene

#make tree

#run paralog 